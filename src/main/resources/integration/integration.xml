<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:http="http://www.springframework.org/schema/integration/http"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
        http://www.springframework.org/schema/integration/http
        http://www.springframework.org/schema/integration/http/spring-integration-http.xsd ">

    <int:channel id="requestChannel"/>
    <int:channel id="replyChannel"/>
    <int:channel id="errorChannel"/>

    <int:gateway id="defaultCalculatorGateway" service-interface="pl.bgora.CalculatorGateway"
                  error-channel="errorChannel" >
        <int:method name="execute" request-channel="requestChannel" reply-channel="replyChannel" />
    </int:gateway>

    <http:outbound-gateway request-channel="requestChannel" reply-channel="replyChannel" http-method="GET"
                           url="http://calculatorweb.herokuapp.com/calculate/{equation}"
                           expected-response-type="java.lang.String"
                           reply-timeout="1000">
        <http:uri-variable name="equation" expression="payload"/>
    </http:outbound-gateway>

    <int:service-activator input-channel="errorChannel" ref="errorHandler" method="handleError"/>

    <!--<int:transformer ref="replayTransformer" input-channel="replyChannel"  method="transform"/>-->


</beans>